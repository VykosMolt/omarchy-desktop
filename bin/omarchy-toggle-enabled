#!/bin/bash

# omarchy:summary=Check if a toggle is enabled (flag file exists)
# omarchy:args=<flag-name>

source "${OMARCHY_PATH:-$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)}/lib/omarchy-paths.sh"

[[ -f "$OMARCHY_STATE_HOME/toggles/$1" ]]
