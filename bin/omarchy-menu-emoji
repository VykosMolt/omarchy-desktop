#!/bin/bash
# omarchy:summary=Launch emojis
# omarchy:group=menu
# omarchy:examples=omarchy menu emoji

source "${OMARCHY_PATH:-$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)}/lib/omarchy-paths.sh"

omarchy_require_ui || exit 1

omarchy-shell shell toggle omarchy.emojis
