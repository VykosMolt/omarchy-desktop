#!/bin/bash

# omarchy:summary=Check if a Hyprland toggle is currently disabled (missing).
# omarchy:args=<flag-name>

source "${OMARCHY_PATH:-$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)}/lib/omarchy-paths.sh"

[[ ! -f "$OMARCHY_STATE_HOME/toggles/hypr/$1.lua" ]]
